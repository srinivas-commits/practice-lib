import { VideosanitizePipe } from './videosanitize.pipe';

describe('VideosanitizePipe', () => {
  it('create an instance', () => {
    const pipe = new VideosanitizePipe();
    expect(pipe).toBeTruthy();
  });
});
